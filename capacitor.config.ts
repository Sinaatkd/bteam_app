import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bteamroyal.app',
  appName: 'Bteam',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
